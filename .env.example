IC_BASE_URL=https://campus.springfield.k12.example.us
IC_DISTRICT=springfield
IC_USERNAME=parent@example.com
IC_PASSWORD=
IC_NAME=Springfield

# Optional: the session is cached between runs so a restart does not
# re-authenticate. Set to false to turn that off.
# IC_SESSION_CACHE=false
# Optional: where that cache lives.
# IC_SESSION_FILE=/path/to/session.json
